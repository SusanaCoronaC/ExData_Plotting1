pipeline {
  agent any
  stages {
    stage('Inicio_environment') {
      steps {
        echo 'Iniciando construcción'
      }
    }

    stage('instalacion') {
      steps {
        sh 'gcc -v'
      }
    }

  }
}